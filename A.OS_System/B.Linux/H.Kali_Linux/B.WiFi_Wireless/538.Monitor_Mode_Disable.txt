REM Disable monitor mode and return to managed mode
DELAY 500
STRING read -p "Interface in monitor mode (e.g. wlan0): " i; sudo ip link set "$i" down && sudo iwconfig "$i" mode managed && sudo ip link set "$i" up && echo "Managed mode restored on $i"
ENTER
