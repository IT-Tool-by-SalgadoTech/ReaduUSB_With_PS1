REM Enable monitor mode using airmon-ng (kills interfering processes first)
DELAY 500
STRING read -p "Interface (e.g. wlan0): " i; sudo airmon-ng check kill; sudo airmon-ng start "$i" && echo "Monitor mode started. New interface is likely ${i}mon"
ENTER
