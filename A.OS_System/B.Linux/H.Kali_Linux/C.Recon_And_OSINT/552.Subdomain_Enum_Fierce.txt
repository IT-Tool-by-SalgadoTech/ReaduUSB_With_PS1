REM Subdomain enumeration using fierce
DELAY 500
STRING read -p "Domain to enumerate subdomains: " d; fierce --domain "$d" 2>/dev/null || echo "fierce not installed. Run: sudo apt install fierce"
ENTER
