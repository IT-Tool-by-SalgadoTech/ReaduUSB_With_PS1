REM Show saved WiFi passwords for known networks (requires root)
DELAY 500
STRING sudo grep -r "psk=" /etc/NetworkManager/system-connections/ 2>/dev/null || sudo cat /etc/wpa_supplicant/wpa_supplicant.conf 2>/dev/null
ENTER
