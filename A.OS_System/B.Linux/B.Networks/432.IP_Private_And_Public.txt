REM Show private and public IP addresses
DELAY 500
STRING ip -4 addr show | grep -oP '(?<=inet\s)\d+(\.\d+){3}/\d+' | grep -v 127; echo "Public IP: $(curl -s https://api.ipify.org 2>/dev/null || curl -s https://ifconfig.me 2>/dev/null)"
ENTER
