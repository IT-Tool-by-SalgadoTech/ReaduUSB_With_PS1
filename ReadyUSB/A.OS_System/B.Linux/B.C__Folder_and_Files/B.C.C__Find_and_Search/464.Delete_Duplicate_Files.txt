REM Find and list duplicate files by hash (requires fdupes)
DELAY 500
STRING read -p "Directory to scan: " d; fdupes -r "$d" 2>/dev/null || echo "fdupes not installed. Run: sudo apt install fdupes"
ENTER
