REM Stop airmon-ng monitor mode and restore managed interface
DELAY 500
STRING read -p "Monitor interface (e.g. wlan0mon): " i; sudo airmon-ng stop "$i" && sudo service NetworkManager restart && echo "Monitor mode stopped."
ENTER
